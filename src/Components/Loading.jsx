import React from "react";

const Loading = () => {
  return (
    <div className="w-full min-h-[50vh] flex items-center justify-center">
      <span className="loading loading-xl loading-spinner text-blue-500"></span>
    </div>
  );
};

export default Loading;